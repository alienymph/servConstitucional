<%- include('partials/header') %>

<div class="card">
  <div class="card-body">
    <h4 class="card-title">Subir PDF</h4>

    <div class="mb-3">
      <label class="form-label">Selecciona un archivo PDF</label>
      <input type="file" id="file" accept="application/pdf" class="form-control" />
      <div class="form-text">Se subirá únicamente el archivo. Los metadatos se pueden completar después desde Gestionar.</div>
    </div>

    <div class="d-flex gap-2">
      <button id="uploadBtn" type="button" class="btn btn-primary">Subir</button>
      <a href="/manage" class="btn btn-secondary">Gestionar</a>
    </div>
  </div>
</div>

<div id="toastContainer" class="position-fixed bottom-0 end-0 p-3" style="z-index:1080"></div>

<%- include('partials/footer') %>
